spring:
    datasource:
        url: jdbc:h2:mem:loop-test;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false;MODE=PostgreSQL
        driver-class-name: org.h2.Driver
        username: sa
        password:

    h2:
        console:
            enabled: false

app:
    jwt:
        secret: test-jwt-secret-key-must-be-at-least-256-bits-long-for-hs256
        expiration-ms: 3600000
