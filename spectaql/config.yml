spectaql:
  targetDir: build/spectaql

introspection:
  schemaFile:
    # BC별 스키마 추가 시 여기에 경로 추가
    # - src/main/resources/schema/task.graphqls
    # - src/main/resources/schema/auth.graphqls

info:
  title: Loop GraphQL API
  description: Loop 프로젝트 GraphQL API 레퍼런스
